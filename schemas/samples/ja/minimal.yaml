# TaskPilot - 最小構成サンプル
# 基本的なメニュー構成の例
# Generated from types.ts (SSoT)

version: "1.0"

menu:
  - label: Build
    icon: "$(package)"
    type: terminal
    command: npm run build

  - label: Test
    icon: "$(beaker)"
    type: terminal
    command: npm test
